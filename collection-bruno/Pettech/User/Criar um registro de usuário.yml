info:
  name: Criar um registro de usuário
  type: http
  seq: 1

http:
  method: POST
  url: "{{servidor}}/user"
  body:
    type: json
    data: |-
      {
        "username": "euhenriquegheno",
        "username": "lulu",
        "password": "#123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
