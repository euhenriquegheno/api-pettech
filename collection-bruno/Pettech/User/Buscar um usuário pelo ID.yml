info:
  name: Buscar um usuário pelo ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{servidor}}/user/:id"
  params:
    - name: id
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
