info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: "{{servidor}}/user/signin"
  body:
    type: json
    data: |-
      {
        "username": "lulu",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
