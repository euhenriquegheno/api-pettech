info:
  name: Inserir uma categoria
  type: http
  seq: 1

http:
  method: POST
  url: "{{servidor}}/category"
  body:
    type: json
    data: |-
      {
        "name": "Categoria 2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
